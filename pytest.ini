[pytest]
markers =
    live_judge: requires ANTHROPIC access via OAuth and live Opus calls (opt-in, costs subscription quota)
    live_provider: calls configured external LLM providers (requires KINGDOM_RUN_LIVE_PROVIDER_TESTS=1)
