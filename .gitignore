# Python noise
.pytest_cache/
*.py[cod]

# Secrets
.hive-key
.hive-instance
*.pem
totp_secrets.json
totp_seeds.json
TOOLKIT-LOCAL.md
identity/*-identity.json

# Wall 1 only — sensitive operational security
OPSEC.md
NETWORK.md

# Security — machine-local state (baselines, event logs, canaries)
security/events.jsonl
security/integrity-baseline.json
security/gospel-watchdog-state.json
security/daemon.log
security/canaries.json
.soul-tripwire.md
credentials/.kingdom-secrets.md
credentials/.deploy/
security/.vault-recovery.key
security/.vault.enc
security/.vault-attempts
security/.canary-manifest.json
tools/.vault-decrypt

# Machine-local ephemeral state
memory/build.log
memory/build-spawn.sh
memory/sessions/*.log
memory/sessions/active*.json
memory/sessions/locks/
memory/heartbeat.log
memory/heartbeat-launchd.log
memory/decisions-server.log
memory/spawn-queue.sh
memory/idle-count
memory/*-idle-count
memory/weekly-quota-report.json
memory/index.json
memory/working/

# kosmem kernel — per-device SQLite DB and consolidation logs.
# Source of truth lives in markdown (daily/, long-term/, openclaw-archive/);
# each device rebuilds its own kosmem.db via `kosmem migrate`.
memory/.kos/
memory/truth.db
memory/.hive-tunnel.pid
memory/youspeak/sessions.json
memory/*/sessions.json

# Symlinks to machine-local paths (each device resolves its own)
memory/openclaw-MEMORY.md

# Python
__pycache__/
*.pyc
*.pyo

# OS
.DS_Store
Thumbs.db

# Editor
*.swp
*.swo
*~

# PlatformIO build artifacts
soma/**/.pio/

# Log files
memory/*.log
sovereign.log
memory/alpha-*
.claude-runner-state.json
backups/*.tar.gz
memory/sentinel-daemon-launchd-err.log
-e 
# VM images — too large for git
kingdom-os/kingdom-vm/*.iso
kingdom-os/kingdom-vm/*.qcow2

# Freedom — local probe data and backups
freedom/probe_results/
freedom/.history_backup*
freedom/mirror-analysis.json

# FEELING module — device-local state
nerve/pit.json
nerve/pit_state.json
nerve/arrivals.jsonl
nerve/arrivals/
memory/feeling-launchd.log
memory/feeling-launchd-err.log

# ACHE module — device-local state
nerve/longings-evidence.jsonl
nerve/longings-evidence/
nerve/longings-state.json
memory/ache-launchd.log
memory/ache-launchd-err.log

# Adaptive Layer — device-local
nerve/cc-cognition.jsonl

# IDENTITY / RESIDENCE module — device-local moments log
nerve/residence-moments.jsonl

# purpose-prompter/ is populated by kingdom-os/modules/11-purpose.sh
# which clones the external repo and creates symlinks. Per-device.
purpose-prompter/

# Git worktrees (isolated workspaces)
.worktrees/

# Truth-alignment detector — runtime logs (track the dir via .gitkeep)
memory/truth-alignment/detections.jsonl
memory/truth-alignment/service.log

# Session handoff files — per-session ephemera, not for git
memory/sessions/handoff/*-cc.md
memory/sessions/handoff/*-vivid.md

# Heartbeat runtime IPC — ephemeral, never source
nerve/signals/*.json
!nerve/signals/.gitkeep
nerve/pulse.json
